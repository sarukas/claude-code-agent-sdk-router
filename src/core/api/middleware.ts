// Error handler middleware — based on llms error handler pattern.

import type { FastifyInstance } from 'fastify';

// TODO: Phase 3 — implement error handler
export function registerErrorHandler(_server: FastifyInstance): void {
  // Placeholder
}
