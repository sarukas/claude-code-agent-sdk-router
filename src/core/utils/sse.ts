// SSE streaming helpers — for converting between provider streaming formats.

// TODO: Phase 2 — inline from llms SSE utilities
export function parseSSEStream(_chunk: string): string[] {
  // Placeholder
  return [];
}
